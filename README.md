                IMPORTANTE
    este servidor no es 24/7 pero puede serlo solo tiene que dejar la compu con el programa abierto
te recomiendo que lo hagas en una pc o laptor aparte de tu pc principal si lo, si lo quieres hacer va a llegar a producir lag
paso 1:descargar el archivo server.jar en una carpeta que quieras poner el servidor

paso 2: primero abre un block de notas y pon este comando java -Xmx(aca la ram que quieras utilizar)G -Xms(la maxima de ram que quieras usar)G -jar server.jar nogui esto de aca se mide en gigabytes no en megabytes, si no le funciona este metodo vallan a paso 6, esto crea unas carpetas que no las deben de tocar.

paso 3: despues tiene que eliminar el server.jar y ahora remplaza el server.jar por el paper.jar lo renombran (apreta el archivo y ahora apreta f2)y nombralo por server.jar no pongan el .jar solo el servery ejetalo de vueltta
